/* SST38 Driver Instance Configuration */
#define DRV_SST38_INDEX                 (0U)
#define DRV_SST38_CLIENTS_NUMBER        (${DRV_SST38_NUM_CLIENTS}U)
#define DRV_SST38_START_ADDRESS         (${FLASH_START_ADDRESS}U)
#define DRV_SST38_CHIP_SELECT           (${CHIP_SELECT}U)
#define DRV_SST38_PAGE_SIZE             (${FLASH_PROGRAM_SIZE}U)
#define DRV_SST38_ERASE_BUFFER_SIZE     (${FLASH_ERASE_SIZE}U)
